from calibration import calibrate, calibrate2
from hotspot_detection import hotspot_detection, EventHandler

if __name__ == "__main__":
    print(calibrate)
    print(calibrate2)
    hotspot_detection(EventHandler())
